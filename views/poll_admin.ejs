<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Votr App</title>
    <%- include('partials/_head')%>

  </head>

  <body>
    <%- include('partials/_nav')%>
    <section class="container">
      <div class="row">
         <section class="total-poll-result col-md-8">
        <section class="poll-info">
          <!-- TODO: Display title, description FROM 'polls'-->
          <h1 class="poll-title"><%= resultTitle.title %></h1>
          <p class="poll-describe">- <%= resultTitle.description %></p>
          <h2 class="winner"><%= winner %>.</h2>
          <% resultRanks.forEach((loser) => { %>
            <h2 class="losers"><%= loser.title %></h2>
          <% }); %>
        </section>
        <section class="poll-choices">
          <!-- TODO: Display all choices FROM 'choices' WHERE poll_id = id-->

        </section>

        <footer class="result-poll-actions">
          <div class="create-another-poll">
            <button type="button" class="btn btn-primary">New poll</button>
          </div>
        </footer>
      </section>

      <section class="single-poll-result col-md-4">
        <!--TODO: Display single person's poll results -->
         <%- include('partials/_single_result')%>
      </section>

      </div>
    </section>
  </body>
</html>
